# Backend (LTI API)

- Run tests: `pnpm test` (from this `backend/` folder).
- Jest + ts-jest: `jest.config.cjs`; test files `*.test.ts` under `src/`.
- Candidate domain: `src/application/validator.ts`, `src/application/services/candidateService.ts`, `src/domain/models/`.

For the full workflow (PRD, BDD, TDD, agentic roles), see the repo root `AGENTS.md` and `.cursor/rules/lti-agent-workflow.mdc`.
